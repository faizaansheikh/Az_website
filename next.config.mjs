/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: ['az-scientificsolutions.com','orbisbv.com','milestonesrl.com','xos.com'],
  },
};

export default nextConfig;